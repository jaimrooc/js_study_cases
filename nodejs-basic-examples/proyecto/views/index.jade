html
	head
		title hola
	body
		h1!=titulo
		p
			- let arreglo=["nodejs", "npm", "jade"];
			- for (var i = arreglo.length - 1; i >= 0; i--)
				p=arreglo[i]
			- arreglo.forEach((elemento)=>{
				p!= "<a href='www.google.com' target='blank'>"+elemento+"</h1>"
			-	});
